package io.coconut.ferrum.utils;

import static io.coconut.ferrum.Launcher.MINECRAFT_DIR;

public class Command {

    /* public static String gameDir = MINECRAFT_DIR;
    public static String assetsDir = MINECRAFT_DIR + "/assets";
    public static String nativesDir = MINECRAFT_DIR + "/natives/" + version;

    public static void buildCommand() {
        ProcessBuilder pb = new ProcessBuilder(
                "java",
                "-Xmx2G",
                "-Xms1G",
                "-Djava.library.path=" + nativesDir,
                "-cp", classPath.toString(),
                mainClass,
                "--username", username,
                "--version", version,
                "--gameDir", gameDir,
                "--assetsDir", assetsDir,
                "--assetIndex", assetIndexId,
                "--uuid", "00000000-0000-0000-0000-000000000000",
                "--accessToken", "0",
                "--userType", "legacy",
                "--versionType", "release"
        );
    }
     */
}
