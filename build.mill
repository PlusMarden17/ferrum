package build
import mill.*
import scalalib.*

object `package` extends ScalaModule {
  def scalaVersion = "3.7.4"
  def mvnDeps = Seq(
    mvn"com.google.code.gson:gson:2.13.2"
  )

  override def mainClass: T[Option[String]] = Some("io.coconut.ferrum.Main")
}
